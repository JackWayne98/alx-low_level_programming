#include "main.h"
#include <unistd.h>
#include <string.h>
/**
 * add - add two integers and returns the result
 *
 * Description: function to add two integer and result the addition
 * @a: receives an integer
 * @b: receives another integer
 * Return: Addition of both integers
 */
int add(int a, int b)
{
	int j = a + b;

	return (j);
}
